1. constructor : __init__(self, user_name, pass_word, db_name, host_name = "localhost") 

2. connect_py_sql(self) : Connects to the database as mentioned in the constructor. Prints appropriate error
                        if connection was not successful or details were wrong

3. run(self, sql_stmt, params = None) : Runs the sql query 'sql_stmt' with parameters if needed

sql_stmt : the sql query to be executed (string)

params : parameters required to be passed to the query which will replace the placeholders in the sql_stmt 
         during execution of query in the form of a tuple

4. run_all(self, sql_stmt, params) : Runs the sql query 'sql_stmt' for multiple sets of parameters

sql_stmt : the sql query to be executed (string)

params : a list of parameters to be passed to the query(list of tuples)

5. get_results(self) : Returns the output of the last executed sql query. If not available, returns the latest
                 available output

6. commit(self) : Updates the remote database with the changes made to the local copy of the database

7. rollback(self) : Rolls back any changes made to the local copy of the remote database

8. get_host_name(self) : Returns the host name

9. get_user_name(self) : Returns the user name

10. get_database_name(self) : Returns the name of the local database